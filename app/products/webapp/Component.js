/* global hasher */
sap.ui.define(['sap/fe/AppComponent'], function(AppComponent) {
    'use strict';

    return AppComponent.extend('Namespace1.products.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
